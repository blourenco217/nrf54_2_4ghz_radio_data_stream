

Enhanced ShockBurst (ESB) is a property radio protocol that runs directly on top of the radio hardware. It’s designed to make wireless communication simpler and more efficient without needing a full BLE stack.